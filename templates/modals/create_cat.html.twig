<!-- Modal -->
<div class="modal fade" id="create_catModal" tabindex="-1" aria-labelledby="#create_catModal" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title text-dark" id="create_catModal">Kategorie erstellen</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body text-dark">
            {{ render(controller('App\\Controller\\NotesController:newCat')) }}
            {{ form_start(form, {'action' : path('newCat')}) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Abbrechen</button>
        <button type="button" class="btn btn-success">Erstellen</button>
      </div>
    </div>
  </div>
</div>